//! `ModrinthService` (Phase 6).
//!
//! Client for the public Modrinth API: search/browse mods, modpacks,
//! resource packs and shaders; project + version metadata; resolving the
//! Modrinth version compatible with an instance's Minecraft version and
//! loader; dependency resolution so a mod's required dependencies get
//! installed alongside it instead of producing a broken instance.
